# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/

# Scratch / pending deletion
_to_delete/

# Credentials and secrets - never commit.
# The real lab credentials file is deliberately kept OUTSIDE this repo;
# these patterns are a second line of defence in case one ever lands inside it.
*.env
secrets/
*.key
*.pem
*.pfx
*.p12
*credential*
*password*
*secret*
okta-api-token*
*token.txt

# Exported IdP/SP metadata can contain tenant identifiers and certificates
*metadata.xml
